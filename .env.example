# Consumed by docker-compose.yml. Copy this to `.env` at the repo root.
# Per-app env files (apps/api/.env, apps/web/.env) hold the application config.
POSTGRES_USER=orderhub
POSTGRES_PASSWORD=orderhub
POSTGRES_DB=orderhub
POSTGRES_PORT=5432
